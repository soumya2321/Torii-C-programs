#include<stdio.h>
int main()
{
    int i,max,n,rem,num,largest=0;
    scanf("%d",&n);
    while(n>0)
    {
        num = n%10;
        if(num>largest)
        {
            largest=num;
        }
        n=n/10;
    }
    printf("%d",largest);
}    
