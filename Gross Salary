#include<stdio.h>
int main()
{
    double x;
    scanf("%lf",&x);
    if(x<=10000)
    printf("%.2lf",x+x*0.8+x*0.2);
    else if(x>10000 && x<=20000)
    printf("%.2lf",x+x*0.9+x*0.25);
    else if(x>20000 && x<=100000)
    printf("%.2lf",x+x*0.95+x*0.3);
    return 0;
}
