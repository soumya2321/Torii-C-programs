#include<stdio.h>
int main()
{
    int b1,b2,b3;
    scanf("%d %d %d",&b1,&b2,&b3);
    int count=0;
    if(b1==0)
    count++;
    if(b2==0)
    count++;
    if(b3==0)
    count++;
     if(count>=2)
     printf("Water filling time");
     else
     printf("Not now");
     return 0;
}
