#include<stdio.h>
int main()
{
    int i, n, z, found = 0;
    scanf("%d", &n);
    int arr[n];
    for(i = 0; i < n; i++)
    {
        scanf("%d", &arr[i]);
    }
    scanf("%d", &z);
    for(i = 0; i < n; i++)
    {
        if(arr[i] == z)
        {
            found = 1;
            break;
        }
    }
    if(found)
        printf("True");
    else
        printf("False");

    return 0;
}
